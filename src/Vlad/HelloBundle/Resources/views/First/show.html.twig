{% block body -%}
    <h1>Список пользователей:</h1>
    <table class="records_list">
        <thead>
        <tr>
            <th>Номер</th>
            <th>Имя</th>
            <th>Фамилия</th>
            <th>Возраст</th>
            <th>Дата добавления</th>
            <th>Управление</th>
        </tr>
        </thead>
        <tbody>
        {#{% for entity in entities %}#}
            <tr>
                {#<td><a href="{{ path('_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>#}
                <td>{{ entity.id }}</td>
                <td>{{ entity.firstname }}</td>
                <td>{{ entity.lastname }}</td>
                <td>{{ entity.age }}</td>
                <td>{% if entity.registered %}{{ entity.registered|date('Y-m-d H:i:s') }}{% endif %}</td>
                {#<td>#}
                    {#<ul>#}
                        {#<li>#}
                            {#<a href="{{ path('vlad_first_show', { 'id': entity.id }) }}">Просмотр</a>#}
                        {#</li>#}
                        {#<li>#}
                            {#<a href="{{ path('_edit', { 'id': entity.id }) }}">Редактирование</a>#}
                        {#</li>#}
                    {#</ul>#}
                {#</td>#}
            </tr>
        {#{% endfor %}#}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('vlad_first') }}">
                Назад к списку пользователей
            </a>
        </li>
    </ul>
{% endblock %}
